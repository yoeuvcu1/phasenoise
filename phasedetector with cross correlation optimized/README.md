# Optimized phase-noise cross-correlation

This is an optimized copy of the phase-noise cross-correlation project.
The original project directory is unchanged.

Run `main` for one simulation or `test_rms_runs` for the batch RMS test.
The phase-noise generator intentionally keeps its per-call seed behavior.